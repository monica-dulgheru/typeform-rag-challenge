# System dependencies (install with sudo apt install):
# sudo apt update
# sudo apt install -y python3.10 python3.10-venv python3.10-dev
# sudo apt install -y build-essential gcc g++ 
# sudo apt install -y libxml2-dev libxslt1-dev
# sudo apt install -y libffi-dev libssl-dev
# sudo apt install -y curl wget git
# sudo apt install -y pkg-config

# Core web framework
fastapi==0.104.1
uvicorn[standard]==0.24.0

# Google Cloud and AI
google-cloud-aiplatform==1.71.1
vertexai==1.71.1
google-generativeai==0.8.5

# Vector database
pinecone==7.3.0

# LLM abstraction
litellm==1.17.0

# Text processing and chunking
langchain-text-splitters==0.2.0

# HTML parsing and text processing
beautifulsoup4==4.12.2
lxml==4.9.3

# Data processing
pandas==2.1.3
numpy==1.24.3

# Configuration and validation
pydantic==2.5.0
pyyaml==6.0.1
packaging==24.2
grpcio-status==1.71.2

# HTTP requests
requests==2.32.5

# Logging and utilities
python-dotenv==1.0.0

# Jupyter/IPython kernel for Cursor/VSCode interactive windows
ipykernel==6.29.0
jupyter==1.0.0
ipython==8.18.1

# To set up the kernel for Cursor/VSCode:
# 1. Activate your virtual environment: source venv_type/bin/activate
# 2. Install the kernel: python -m ipykernel install --user --name=venv_type --display-name="Python (venv_type)"
# 3. In Cursor/VSCode, open Command Palette (Ctrl+Shift+P) and select "Python: Select Interpreter"
# 4. Choose the interpreter from your venv_type environment
# 5. For interactive windows, the kernel should automatically use the selected interpreter
